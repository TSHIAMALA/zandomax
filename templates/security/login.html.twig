<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - Marché Central Zando</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        .hero-section {
            background: linear-gradient(135deg, #1e3a5f 0%, #2c4a6f 50%, #1e3a5f 100%);
            position: relative;
            overflow: hidden;
        }
        .hero-bg-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('/images/market-hero-bg.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            opacity: 1;
        }
        .content-overlay {
            position: relative;
            z-index: 10;
        }
    </style>
</head>
<body class="min-h-screen bg-gray-100">
    <div class="hero-section min-h-screen flex flex-col">
        <!-- Background Image -->
        <div class="hero-bg-image"></div>

        <!-- Top Navigation -->
        <nav class="content-overlay flex items-center justify-between px-4 md:px-8 py-3 md:py-4">
            <div class="flex items-center space-x-2 md:space-x-3">
                <div class="w-8 h-8 md:w-10 md:h-10 bg-yellow-500 rounded-lg flex items-center justify-center shadow-lg flex-shrink-0">
                    <i class="fas fa-store text-white text-base md:text-xl"></i>
                </div>
                <span class="text-white font-semibold text-sm md:text-lg whitespace-nowrap">Marché Central - Zando</span>
            </div>
            <div class="flex items-center space-x-3 md:space-x-6">
                <a href="#" onclick="document.getElementById('loginModal').classList.remove('hidden'); return false;" class="text-white/80 hover:text-white text-xs md:text-sm transition">Connexion</a>
                <a href="#" class="text-white/80 hover:text-white text-xs md:text-sm transition hidden sm:inline">Annonces</a>
                <button class="w-7 h-7 md:w-8 md:h-8 bg-white/10 rounded-lg flex items-center justify-center text-white hover:bg-white/20 transition">
                    <i class="fas fa-lock text-sm md:text-base"></i>
                </button>
            </div>
        </nav>

        <!-- Main Content -->
        <div class="content-overlay flex-1 flex items-center justify-center px-4 py-8">
            <div class="text-center max-w-3xl">
                <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-4 leading-tight drop-shadow-lg">
                    Plateforme Digitale<br>Marché Central - Zando
                </h1>
                <p class="text-white/90 text-sm sm:text-base md:text-lg mb-6 md:mb-8 max-w-2xl mx-auto drop-shadow px-2">
                    Gestion facile, développement, paiement des commerçants et contrôle de paiements
                </p>
                
                <button onclick="document.getElementById('loginModal').classList.remove('hidden')" 
                        class="bg-yellow-500 hover:bg-yellow-600 text-white font-semibold px-8 md:px-10 py-3 md:py-4 rounded-lg transition transform hover:scale-105 shadow-xl text-sm md:text-base">
                    Commencer Ici
                </button>
            </div>
        </div>

        <!-- Footer Partners -->
        <div class="content-overlay bg-white py-4 md:py-6 shadow-lg">
            <div class="container mx-auto px-4 md:px-8">
                <div class="flex flex-wrap items-center justify-center gap-4 md:gap-8 lg:gap-12 text-gray-600">
                    <div class="flex items-center space-x-2">
                        <span class="font-bold text-lg md:text-2xl text-blue-900">APDNK</span>
                    </div>
                    <div class="text-xs md:text-sm text-center">
                        <div class="font-semibold">Gouvernement</div>
                        <div class="text-xs text-gray-400">Numérique</div>
                    </div>
                    <div class="flex items-center space-x-2">
                        <div class="w-8 h-8 md:w-10 md:h-10 bg-blue-900 rounded flex items-center justify-center text-white">
                            <i class="fas fa-university text-sm md:text-base"></i>
                        </div>
                        <div class="text-xs md:text-sm">
                            <div class="font-semibold">Banque</div>
                            <div class="text-xs text-gray-400">Centrale</div>
                        </div>
                    </div>
                    <div class="text-xs md:text-sm text-center hidden sm:block">
                        <div class="font-semibold">Telecom</div>
                        <div class="text-xs text-gray-400">DRC</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Login Modal -->
    <div id="loginModal" class="hidden fixed inset-0 bg-black/60 flex items-center justify-center z-50 p-4 backdrop-blur-sm">
        <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full p-8 relative animate-fade-in">
            <button onclick="document.getElementById('loginModal').classList.add('hidden')" 
                    class="absolute top-4 right-4 text-gray-400 hover:text-gray-600 transition">
                <i class="fas fa-times text-xl"></i>
            </button>

            <div class="text-center mb-8">
                <div class="w-16 h-16 bg-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg">
                    <i class="fas fa-store text-white text-2xl"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Connexion</h2>
                <p class="text-gray-500">Accédez à votre espace de gestion</p>
            </div>

            {% for message in app.flashes('success') %}
                <div class="mb-6 bg-green-50 border-l-4 border-green-500 p-4 rounded-r-lg">
                    <div class="flex items-center">
                        <i class="fas fa-check-circle text-green-500 mr-3"></i>
                        <p class="text-green-700 text-sm">{{ message }}</p>
                    </div>
                </div>
            {% endfor %}

            {% if error %}
                <div class="mb-6 bg-red-50 border-l-4 border-red-500 p-4 rounded-r-lg">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
                        <p class="text-red-700 text-sm">{{ error.messageKey|trans(error.messageData, 'security') }}</p>
                    </div>
                </div>
            {% endif %}

            <form method="post" class="space-y-5">
                <div>
                    <label for="username" class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-user mr-2 text-gray-400"></i>Email / Identifiant
                    </label>
                    <input type="text" 
                           id="username" 
                           name="_username" 
                           value="{{ last_username }}" 
                           required 
                           autofocus
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
                           placeholder="Votre identifiant">
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-lock mr-2 text-gray-400"></i>Mot de Passe
                    </label>
                    <input type="password" 
                           id="password" 
                           name="_password" 
                           required
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
                           placeholder="••••••••">
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center">
                        <input type="checkbox" name="_remember_me" class="w-4 h-4 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
                        <span class="ml-2 text-sm text-gray-600">Se souvenir de moi</span>
                    </label>
                    <a href="#" class="text-sm text-blue-600 hover:text-blue-700 font-medium">Mot de passe oublié?</a>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                
                <button type="submit" 
                        class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-lg font-semibold transition transform hover:scale-[1.02] shadow-lg">
                    <i class="fas fa-sign-in-alt mr-2"></i>Se connecter
                </button>
            </form>

            <div class="mt-6 text-center">
                <p class="text-sm text-gray-600">
                    Pas encore de compte? 
                    <a href="{{ path('app_register') }}" class="text-blue-600 hover:text-blue-700 font-medium">Inscrivez-vous</a>
                </p>
            </div>

            <div class="mt-6 pt-6 border-t border-gray-200">
                <div class="flex items-center justify-center space-x-4 text-xs text-gray-400">
                    <span>APDNK</span>
                    <span>•</span>
                    <span>Gouvernement</span>
                    <span>•</span>
                    <span>Banque Centrale</span>
                </div>
            </div>
        </div>
    </div>

    <script>
        {% if error %}
            document.getElementById('loginModal').classList.remove('hidden');
        {% endif %}
        
        // Ouvrir le modal si un message de succès est affiché
        if (document.querySelector('.bg-green-50')) {
            document.getElementById('loginModal').classList.remove('hidden');
        }
    </script>
</body>
</html>
