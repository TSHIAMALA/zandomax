{% extends 'merchant_layout.html.twig' %}

{% block title %}Paiement Sécurisé - ZANDO Market{% endblock %}

{% block content %}
<div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
        <div class="mx-auto h-12 w-12 bg-purple-600 rounded-xl flex items-center justify-center text-2xl text-white">
            💳
        </div>
        <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
            Paiement Sécurisé
        </h2>
        <p class="mt-2 text-center text-sm text-gray-600">
            Finalisez votre transaction pour la référence <strong>{{ payment.bankingTransactionId }}</strong>
        </p>
    </div>

    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
        <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
            <div class="mb-6">
                <div class="flex justify-between items-center border-b pb-4">
                    <span class="text-gray-500">Montant à payer</span>
                    <span class="text-2xl font-bold text-gray-900">{{ payment.amount|number_format(0, ',', ' ') }} {{ payment.currency }}</span>
                </div>
            </div>

            <form method="post">
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Moyen de paiement</label>
                        <div class="grid grid-cols-2 gap-4">
                            <label class="border rounded-lg p-4 flex items-center cursor-pointer hover:bg-gray-50 has-[:checked]:border-purple-500 has-[:checked]:bg-purple-50">
                                <input type="radio" name="method" value="mobile_money" class="mr-3 text-purple-600" checked>
                                <div>
                                    <span class="block font-medium">Mobile Money</span>
                                    <span class="text-xs text-gray-500">M-Pesa, Orange, Airtel</span>
                                </div>
                            </label>
                            <label class="border rounded-lg p-4 flex items-center cursor-pointer hover:bg-gray-50 has-[:checked]:border-purple-500 has-[:checked]:bg-purple-50">
                                <input type="radio" name="method" value="card" class="mr-3 text-purple-600">
                                <div>
                                    <span class="block font-medium">Carte Bancaire</span>
                                    <span class="text-xs text-gray-500">Visa, Mastercard</span>
                                </div>
                            </label>
                        </div>
                    </div>

                    <div class="pt-4">
                        <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-purple-600 hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500">
                            <i class="fas fa-lock mr-2"></i> Payer {{ payment.amount|number_format(0, ',', ' ') }} {{ payment.currency }}
                        </button>
                    </div>
                    
                    <div class="text-center">
                        <a href="{{ path('merchant_payments') }}" class="text-sm text-gray-500 hover:text-gray-900">
                            Annuler et retourner
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
