<nav class="bg-white shadow-sm mb-6">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-14">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <h1 class="text-xl font-bold text-gray-900">ZANDO Market</h1>
                    <span class="ml-2 px-2 py-0.5 bg-purple-100 text-purple-700 text-xs font-semibold rounded-full">Portail Marchand</span>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-4">
                    <a href="{{ path('merchant_dashboard') }}" class="{% if app.request.get('_route') == 'merchant_dashboard' %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-home mr-1 text-xs"></i>Tableau de Bord
                    </a>
                    <a href="{{ path('merchant_spaces') }}" class="{% if 'spaces' in app.request.get('_route') or 'reserve' in app.request.get('_route') %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-store mr-1 text-xs"></i>Espaces
                    </a>
                    <a href="{{ path('merchant_reservations') }}" class="{% if 'reservations' in app.request.get('_route') %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-calendar-check mr-1 text-xs"></i>Réservations
                    </a>
                    <a href="{{ path('merchant_contract') }}" class="{% if 'contract' in app.request.get('_route') %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-file-contract mr-1 text-xs"></i>Contrat
                    </a>
                    <a href="{{ path('merchant_documents') }}" class="{% if 'documents' in app.request.get('_route') %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-file-alt mr-1 text-xs"></i>Documents
                    </a>
                    <a href="{{ path('merchant_payments') }}" class="{% if 'payments' in app.request.get('_route') %}border-purple-500 text-gray-900{% else %}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{% endif %} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        <i class="fas fa-credit-card mr-1 text-xs"></i>Paiements
                    </a>
                </div>
            </div>
            <div class="flex items-center space-x-3">
                <span class="text-sm text-gray-700">{{ app.user.username }}</span>
                <a href="{{ path('app_logout') }}" class="text-sm text-gray-500 hover:text-gray-700">
                    <i class="fas fa-sign-out-alt mr-1"></i>Déconnexion
                </a>
            </div>
        </div>
    </div>
</nav>
